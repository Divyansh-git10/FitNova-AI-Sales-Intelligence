"""Versioned prompt templates for the analysis engine (Phase 4).

Each template file will carry an explicit version string (e.g.
`issue_detection_v1.txt`), which `llm_client.py` records as
`prompt_version` on every `llm_inference_logs` row — see docs Section 12.
"""
